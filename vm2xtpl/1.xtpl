{{#if (a)}}
    if a
{{else}}
    {{#if (b)}}
        elif b
    {{else}}
        {{#if (c)}}
            elif c
        {{else}}
            {{#if (d)}}
                elif d
            {{else}}
                last el
            {{/if}}
        {{/if}}
    {{/if}}
{{/if}}